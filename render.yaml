services:
  - type: web
    name: litetrace-dashboard
    env: node
    buildCommand: npx pnpm@9.15.0 install && npx pnpm@9.15.0 build
    startCommand: npx pnpm@9.15.0 --filter @litetrace/dashboard start
