{
  "name": "@prod-own/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/helmet": "^13.0.2",
    "@prod-own/config": "workspace:*",
    "@prod-own/db": "workspace:*",
    "@prod-own/observability": "workspace:*",
    "@prod-own/queue": "workspace:*",
    "@prod-own/types": "workspace:*",
    "fastify": "^5.3.3"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
