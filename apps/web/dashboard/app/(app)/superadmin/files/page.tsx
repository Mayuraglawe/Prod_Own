import React from 'react';
import { SuperAdminFiles } from '../../../../components/superadmin-files';

export const metadata = {
  title: 'Super Admin Files & System Documentation | LiteTrace',
  description: 'Central super admin repository storing core system analyses, architecture specifications, governance guidelines, and deployment manifests.',
};

export default function SuperAdminFilesPage() {
  return <SuperAdminFiles />;
}
