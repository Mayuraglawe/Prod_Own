# Hoist all packages to the root node_modules so sub-packages don't get
# their own node_modules folders. This matches the npm/yarn "flat" install
# behaviour while keeping pnpm's content-addressable global store.
#
# Trade-off: you lose strict dependency isolation (packages can accidentally
# import things they didn't declare), but the structure looks simpler.
shamefully-hoist=true
